# AirlineOperationAnalysis

Workspace for the Airline Operation Analysis capstone project.

DATA/ contains CSVs: `airlines.csv`, `airports.csv`, `flights.csv`.

How to use:

- Create a Python virtualenv and install required packages.
- Add a remote with `git remote add origin <url>` and push.
